main(){
/**
 *箭头函数只能写一句话，这和es6中的箭头函数有点区别，

 *闭包： 函数里面嵌套函数，并且返回里面的函数
 *  全局变量：会常驻内存，容易污染全局
 *  局部变量：不会常驻内存，会被垃圾机制回收，不会污染全局
 * 
 *如果想让一个变量常驻内存，而且不污染全局，只能用闭包实现，闭包也是这个目的

  
 *   
*/ 






















}  