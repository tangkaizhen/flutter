main(){
  
  /**
   * 集合类型List Set Map
   * 
   * List属性：
   *    isEmpty：是否为空
   *    isNotEmpty:是否不为空
   *    reversed:数组翻转
   * 
   * List方法： 
   *    add：增加元素,增加一个
   *    addAll:增加元素,增加多个，传入的是一个数组（主要的功能是拼接数组）
   *    indexOf：查找某个元素的索引
   *    remove：删除某个元素
   *    removeAt：删除某个索引的元素
   *    fillRange：修改
   *    insert（index，value）：指定位置插入数据
   *    insertAll：插入多个数据
   */





























}  