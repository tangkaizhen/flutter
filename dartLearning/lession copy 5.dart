main(){
  
  /**
   * 集合类型List Set Map
   * 
   * List属性：
   *    length:长度
   *    isEmpty：是否为空
   *    isNotEmpty:是否不为空
   *    reversed:数组翻转
   * 
   * List方法： 
   *    add：增加元素,增加一个,给数组增加元素只能通过add，不能通过索引
   *    addAll:增加元素,增加多个，传入的是一个数组（主要的功能是拼接数组），类似于concat
   *    remove：删除某个元素，传入具体的值
   *    removeAt：删除某个索引的元素，传入具体的索引 
   *    fillRange：修改
   *    insert（index，value）：指定位置插入数据，插入一个
   *    insertAll:插入多个数据，插入多个 
   */


    var arr=new List();
    arr.addAll([1,2]);
    print(arr);


























}  